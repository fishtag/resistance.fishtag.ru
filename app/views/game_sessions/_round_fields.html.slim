.nested-fields
  = f.association :participants, collection: game_session.players, as: :check_boxes
  = f.input :winner, include_blank: false, as: :radio_buttons
  = link_to_remove_association 'Delete', f
